# Dossier de lecture des maquettes IFC (sandbox). Si absent, chemins absolus acceptés.
AUDIT_INPUT_DIR=/Users/stani/code/MCP/audit_in

# Dossier d'écriture des JSON — DOIT être aligné sur celui d'audit-bim-i3f
# pour que import_preliminary_findings retrouve les fichiers.
AUDIT_OUTPUT_DIR=/Users/stani/code/MCP/audit_out
